let serveHost: string | undefined = undefined; if (process.env.NODE_ENV === "production") { if (process.env.RENDER_EXTERNAL_URL) { serveHost = process.env.RENDER_EXTERNAL_URL; } else if (process.env.REPLIT_DOMAINS) { serveHost = `https://${process.env.REPLIT_DOMAINS.split(",")[0]}`; } else if (process.env.RENDER_EXTERNAL_HOSTNAME) { serveHost = `https://${process.env.RENDER_EXTERNAL_HOSTNAME}`; } else { console.warn('[Inngest] Warning: No external URL configured in production. Set RENDER_EXTERNAL_URL or REPLIT_DOMAINS environment variable.'); } } else { serveHost = baseInternalUrl; }